<%= form_for(@trucking, :html => { :class => 'form-horizontal' }) do |f| %>
  <% if @trucking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trucking.errors.count, "error") %> prohibited this trucking from being saved:</h2>

      <ul>
      <% @trucking.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name, :class => "control-label" %>
    <div class="controls">
        <%= f.text_field :name %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :address, :class => "control-label" %>
    <div class="controls">
      <%= f.text_field :address %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :phone, "Phone Number", :class => "control-label" %>
    <div class="controls">
      <%= f.text_field :phone %>
    </div>
  </div>

  <div class="control-group">
    <div class="controls">
      <%= f.submit :class => "btn btn-primary" %>
    </div>
  </div>
<% end %>
